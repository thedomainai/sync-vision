## **1. プロジェクト概要**

### **1.1 プロダクト名**

**SyncVision (シンクビジョン)**

### **1.2 プロダクトの目的**

会議中の議論をリアルタイムで可視化し、参加者間のメンタルモデル（認識の枠組み）の不一致を解消するWebアプリケーションです。

「ある人は4象限で重要度を考えているのに、ある人はタスクの処理順序（カンバン）を考えている」といった認識のズレを防ぎ、議論をスムーズに収束させることを目的としています。

### **1.3 解決する課題**

- **議論の空中戦**: 言葉だけのやり取りで認識が噛み合わない状態を解消する。
- **視点の固定化**: 多角的な視点（重要度 vs 緊急度、ToDo vs Done）を瞬時に切り替えられない問題を解決する。
- **コンテキストの喪失**: 複数の議題を行き来する際に、どの文脈の話か分からなくなる状況を防ぐ。

## **2. 要件定義・仕様書**

### **2.1 主要機能一覧**

| **機能カテゴリ** | **機能名** | **詳細** |
| --- | --- | --- |
| **可視化ビュー** | **マトリクスビュー** | 2軸（重要度×緊急度）の4象限でアイテムを分類・表示する。 |
|  | **カンバンビュー** | プロセス（未着手〜完了）のフェーズごとにアイテムを管理する。 |
|  | **リストビュー** | 全アイテムを表形式で一覧表示し、詳細確認や削除を行う。 |
| **アイテム操作** | **作成** | テキスト入力により新しいアイデア（カード）を作成する。 |
|  | **ドラッグ&ドロップ** | マウス操作で象限やステータス間を移動させる。 |
|  | **Inbox (未整理)** | どの分類にも属さない一時的な置き場を提供する。 |
| **議論管理** | **マルチページ** | 議題（トピック）ごとに独立したワークスペースを持つ。 |
|  | **プログレスバー** | 全議題の進行状況（完了、現在進行中、未着手）を上部に可視化する。 |
| **ログ連携** | **会議ログ表示** | 左サイドバーに会議の発言ログを表示（現状はモック）。 |
|  | **コンテキストジャンプ** | ログをクリックすると、その発言が行われた議題ページへ自動遷移する。 |

### **2.2 画面構成 (UI仕様)**

画面は大きく分けて以下の4つのエリアで構成される **3カラム + ヘッダー/フッター** レイアウトです。

1. **Top Navigation (プログレスバー)**
- 全議題をステップ形式で表示。
- 現在のステップは拡大され「Doing Now」と表示。
- 完了したステップはチェックマークに変化。
1. **Left Sidebar (会議ログ)**
- 時系列のチャットUI。
- 現在のアクティブな議題と紐付いているログはハイライト表示。
1. **Main Content (ワークスペース)**
- 中央のメイン操作エリア。
- 上部にビュー切り替えタブ（マトリクス / カンバン / リスト）。
- 下部にアイテム追加フォームとInbox（未整理トレイ）。
1. **Right Sidebar (議題リスト)**
- 議題の切り替え、新規議題の追加を行うナビゲーション。
- 各議題に含まれるアイテム数を表示。

### **2.3 インタラクション仕様**

- **ビュー切り替え**:
- ボタンクリックで即座に表示モードを変更。データ（アイテム）は共通で、見せ方だけが変わる。
- **ドラッグ&ドロップの挙動**:
- **マトリクスモード時**: ドロップした象限に応じて quadrant プロパティ（q1〜q4）を更新。
- **カンバンモード時**: ドロップした列に応じて status プロパティ（todo, doing等）を更新。
- **Inboxへのドロップ**: 分類を解除（null化）して未整理に戻す。

## **3. 技術アーキテクチャ**

### **3.1 技術スタック**

- **Frontend Framework**: React 18+
- **Language**: JavaScript (JSX) ※将来的にTypeScriptへの移行を推奨
- **Styling**: Tailwind CSS (Utility-first CSS)
- **Icons**: lucide-react
- **Build Tool**: (想定: Vite または Create React App)

### **3.2 外部依存ライブラリ (package.json想定)**

{

"dependencies": {

"react": "^18.2.0",

"react-dom": "^18.2.0",

"lucide-react": "^0.263.1",

"clsx": "^2.0.0",        // (推奨) クラス名条件分岐用

"tailwind-merge": "^1.14.0" // (推奨) Tailwindクラスのマージ用

}

}

## **4. プログラム詳細設計**

### **4.1 ディレクトリ構造案**

現在は単一ファイル (SyncVision.jsx) ですが、本格開発に向けて以下の構造への分割を推奨します。

src/

├── components/

│   ├── common/

│   │   ├── Card.jsx          // ドラッグ可能なアイテムカード

│   │   └── Button.jsx

│   ├── layout/

│   │   ├── SidebarLeft.jsx   // 会議ログ

│   │   ├── SidebarRight.jsx  // ページリスト

│   │   └── ProgressBar.jsx   // 上部進捗バー

│   └── views/

│       ├── MatrixView.jsx    // 4象限ビュー

│       ├── KanbanView.jsx    // カンバンビュー

│       └── ListView.jsx      // リストビュー

├── hooks/

│   └── useDragDrop.js        // D&Dロジックの分離

├── types/

│   └── index.js              // 定数・型定義

├── App.jsx                   // メインコンテナ

└── main.jsx                  // エントリーポイント

### **4.2 データモデル (Data Structures)**

### **Page (議題)**

{

id: string,       // 一意のID (例: "p1")

title: string,    // 議題のタイトル

items: Item[]     // この議題に属するアイテムの配列

}

### **Item (カード/タスク)**

このオブジェクトがアプリケーションの核となります。1つのオブジェクトが複数のビューの状態（位置情報）を保持します。

{

id: string,          // 一意のID (例: "101")

content: string,     // 表示テキスト

// マトリクスビュー用の状態

quadrant: 'q1' | 'q2' | 'q3' | 'q4' | null,

// カンバンビュー用の状態

status: 'backlog' | 'todo' | 'doing' | 'done' | null

}

### **定数定義 (Constants)**

- **QUADRANTS**: 4象限の定義（ID, ラベル, 説明, 色設定）。
- **KANBAN_COLUMNS**: カンバン列の定義（ID, ラベル, 色設定）。

### **4.3 状態管理 (State Management)**

現状は App コンポーネント内の useState で一元管理しています。

1. **pages**: 全データのマスター。INITIAL_PAGES で初期化。
- データの更新（アイテム追加・移動・削除）は、この pages 配列の中の、activePageId に一致する page オブジェクト内の items 配列を更新することで行います。
1. **activePageId**: 現在表示している議題のID。
2. **viewMode**: 現在のビューモード (matrix | kanban | list)。
3. **draggedItemId**: ドラッグ中のアイテムID。D&D操作の一時保存用。

### **4.4 ドラッグ&ドロップの実装ロジック**

HTML5 標準の Drag and Drop API を使用しています。

1. **Drag Start (onDragStart)**:
- ドラッグされたアイテムのIDを draggedItemId stateに保存。
- e.dataTransfer.effectAllowed = 'move' を設定。
1. **Drag Over (onDragOver)**:
- e.preventDefault() を呼び出し、ドロップ可能なエリアであることをブラウザに通知。
1. **Drop (onDrop)**:
- ドロップ先のエリア（象限ID または カンバン列ID）と、現在のビューモード（matrix or kanban）を受け取る。
- pages stateを更新し、対象アイテムの quadrant または status プロパティを書き換える。
- **重要**: マトリクスで移動しても、カンバンのステータスは維持されます（逆も然り）。これにより「重要だが未着手」といった複合的な状態を保持できます。

## **5. 実装・拡張ガイド**

### **5.1 CSS / スタイリングのポイント**

- **グリッドレイアウト**: マトリクスビューでは grid-cols-2 grid-rows-[auto_1fr_1fr] を使用して、ヘッダー行＋2x2のエリアを構築しています。
- **レスポンシブ**: 基本的にデスクトップ利用を想定していますが、flex-1 や overflow-hidden を多用し、画面サイズに合わせてコンコンテンツが伸縮するように設計されています。
- **アニメーション**: lucide-react アイコンや、Tailwindの transition-all, animate-pulse を使用して、直感的なフィードバック（現在地、ドラッグ可否など）を表現しています。

### **5.2 今後の拡張に向けたTo-Do**

1. **TypeScript化**:
- Item や Page の型定義を厳格に行い、保守性を高める。
1. **永続化**:
- 現在はリロードすると初期データに戻ります。Firebase Firestore や LocalStorage への保存機能の実装が必要です。
1. **リアルタイム通信**:
- 会議参加者全員が同じ画面を見られるよう、WebScoket (Socket.io) や Firebase Realtime Database の導入。
1. **音声認識連携**:
- 左側の「会議ログ」をモックから、Web Speech API等を使った実際の音声文字起こし機能へ置換。

## **6. セットアップ手順 (ローカル開発)**

1. **リポジトリのクローン**:
    
    git clone <repository-url>
    
    cd sync-vision
    
2. **依存パッケージのインストール**:
    
    npm install
    
    # または
    
    yarn install
    
3. **開発サーバーの起動**:
    
    npm run dev
    
    # または
    
    yarn dev
    
4. **ビルド**:
    
    npm run build